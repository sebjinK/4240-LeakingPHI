<%- include('partials/header', { user: user }) %>

<div class="row justify-content-center">
  <div class="col-md-8">
    <div class="card shadow soft-card">
      <div class="card-body">
        <h2 class="mb-3">Baseline Wellness Goals</h2>
        <p class="text-muted">
          Answer a few quick questions so we understand what you're using this app for.
          These help shape future suggestions and difficulty levels.
        </p>

        <% if (errors.length) { %>
          <div class="alert alert-danger">
            <ul class="mb-0">
              <% errors.forEach(e => { %>
                <li><%= e.msg %></li>
              <% }) %>
            </ul>
          </div>
        <% } %>

        <form action="/baseline" method="POST" novalidate>
          <div class="mb-3">
            <label class="form-label">How many hours of sleep are you aiming for each night?</label>
            <input
              type="number"
              step="0.5"
              min="3"
              max="12"
              name="goalSleep"
              class="form-control"
              value="<%= old.goalSleep || '' %>"
              required
            />
          </div>

          <div class="mb-3">
            <label class="form-label">How many cups of water per day is your goal?</label>
            <input
              type="number"
              min="1"
              max="30"
              name="goalWater"
              class="form-control"
              value="<%= old.goalWater || '' %>"
              required
            />
          </div>

          <div class="mb-3">
            <label class="form-label">How many minutes of exercise per day is your goal?</label>
            <input
              type="number"
              min="0"
              max="300"
              name="goalExerciseMinutes"
              class="form-control"
              value="<%= old.goalExerciseMinutes || '' %>"
              required
            />
          </div>

          <div class="mb-3">
            <label class="form-label">
              On a 1–10 scale, what average daily mood are you hoping to maintain?
              <span class="form-text">(1 = very low, 10 = fantastic)</span>
            </label>
            <input
              type="number"
              min="1"
              max="10"
              name="goalMood"
              class="form-control"
              value="<%= old.goalMood || '' %>"
              required
            />
          </div>

          <div class="mb-3">
            <label class="form-label">
              Preferred goal difficulty
              <span class="badge bg-secondary ms-2" data-difficulty-label>
                <%= old.difficultyPreference || 5 %>
              </span>
            </label>
            <input
              type="range"
              min="1"
              max="10"
              name="difficultyPreference"
              value="<%= old.difficultyPreference || 5 %>"
              class="form-range"
              data-difficulty-slider
            />
            <div class="form-text">
              1 = very easy, 10 = very challenging. We’ll use this to tune future suggestions.
            </div>
          </div>

          <button class="btn btn-primary w-100" type="submit">
            Save Baseline &amp; Open Dashboard
          </button>
        </form>
      </div>
    </div>
  </div>
</div>

<%- include('partials/footer') %>